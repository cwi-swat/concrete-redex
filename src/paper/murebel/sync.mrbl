
entity Foo {
  value: int

  init
    on init(): initialized this.value = 0;    
    
  state initialized { 
    on set(v: int) this.value = v;
  }
    

  
}

let foo = new Foo in {
  foo.init();
  par {
    foo.set(1);
    foo.set(2);
    foo.set(3);
  }
}
   
     
